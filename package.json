{
  "name": "lunt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sistema inteligente de analisis y gestion de costos de construccion",
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "preview": "cd web && npm run preview",
    "lint": "cd web && npm run lint",
    "test": "cd web && npm run test",
    "setup:web": "cd web && npm install",
    "setup:all": "npm run setup:web"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
